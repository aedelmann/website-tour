# 2026-08-21

- Charge Stats map empty after tesla-charging-sync (updatedAt 2026-08-21T19:46:03Z): all 46 sites + lastStop had null lat/lng. Root cause path: PR #24 unsorted charging_history retry returns sessions without siteEntryLocation/siteAddress; aggregateSnapshot only read those fields and overwrote Blobs.
- Fix on `cursor/restore-supercharger-pins-a61b`: parse alternate Tesla location keys; before setSnapshot, copy lat/lng/city/country from prior Blobs and/or static/data/charging-stats.json by site name (never invent coords).
- After PR #26 merge + 19:57 sync (46/46 coords), `/api/charging-stats` still served the 19:46 zero-pin body via Netlify Edge (`Cache-Control: public,max-age=300`). Homepage + `/charging/` fetch without `cache: 'no-store'`. Fix on `cursor/charging-stats-cache-bust-e368`: fetch `no-store`, live healthy snapshot max-age=60, zero-pin live snapshot `no-store`, bump JS `?v=cost4`.
- Tesla OAuth: added `energy_device_data` to SCOPES (and partnerAccessToken now uses SCOPES); user re-consent likely needed after deploy for refresh token to pick up the new scope.
